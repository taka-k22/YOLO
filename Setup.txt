CUDAによるGPU推論（3.11）
echo yolo-py311/ >> .gitignore
py -3.11 -m venv yolo-py311
yolo-py311\Scripts\activate
pip install --upgrade pip setuptools
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install ultralytics opencv-python Flask requests
pip freeze > requirements.txt


CPU推論（3.14）
echo yolo-py314-clean/ >> .gitignore
py -3.14 -m venv yolo-py314
yolo-py314\Scripts\activate
pip install --upgrade pip setuptools
pip install ultralytics opencv-python ipython pillow
pip freeze > requirements.txt


DeepSORT（3.11）
echo deepsort-py311/ >> .gitignore
py -3.11 -m venv deepsort-py311
deepsort-py311\Scripts\activate
pip install --upgrade pip "setuptools<81"
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install ultralytics opencv-python Flask requests deep-sort-realtime
pip install -r face-recognition-requirements.txt
pip freeze > requirements.txt
